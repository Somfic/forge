{
  "name": "forge-frontend",
  "private": true,
  "workspaces": [
    "apps/*",
    "../modules/*/frontend"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build"
  },
  "devDependencies": {}
}
